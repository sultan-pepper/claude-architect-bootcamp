# Summarise Service

Read `src/$ARGUMENTS/main.py`. List all HTTP endpoints (method, path, handler function). List all public functions with their signatures. Summarise the service's responsibilities in two sentences.
